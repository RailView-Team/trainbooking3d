# Train Seat Booking

The database layer is documented in [database/README.md](database/README.md). Start with [database/schema.sql](database/schema.sql), then run the four import/seed scripts in the order shown there.
